# Legacy Experiment Families

This directory holds older `exp_*` or `epx_*` families that are preserved for traceability but are no longer part of the main repo path.

These families are not considered active official surfaces and should not be used as the default entrypoint for current work.

Promoted or still-referenced families stay under `families/*`, not in this archive.
