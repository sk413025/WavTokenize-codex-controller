# Agents Registry

This directory holds controller metadata for mapping controller-layer agents onto native Codex roles.

- `registry.json` is an internal mapping table for the thin runtime.
- `AGENTS.md` remains the repo policy source of truth.
- `registry.json` is not a primary entrypoint for starting work in the repo.
