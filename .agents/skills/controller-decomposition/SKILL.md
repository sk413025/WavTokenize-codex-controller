---
name: controller-decomposition
description: Compatibility alias for `experiment-decomposition`.
---

Prefer `experiment-decomposition` for new work.

This compatibility skill exists so older notes still resolve.
Use the same workflow as `experiment-decomposition`:
- split the task by native Codex role
- prefer parallel exploration first
- keep ownership explicit
- avoid adding runtime code when Markdown or skills are enough
