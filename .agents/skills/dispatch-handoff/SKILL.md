---
name: dispatch-handoff
description: Compatibility alias for `native-handoff`.
---

Prefer `native-handoff` for new work.

This compatibility skill exists so older notes still resolve.
Use the same workflow as `native-handoff`:
- define the owner role and expected output
- hand off through native Codex collaboration only
- avoid repo-side packet or queue concepts
